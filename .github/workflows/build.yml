name: Build VPN Subscription

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Prepare folders
        run: |
          mkdir -p build
          rm -f build/output.txt
          touch build/output.txt

      - name: Download subscriptions
        run: |
          while IFS= read -r url || [ -n "$url" ]; do
            if [ -z "$url" ]; then
              continue
            fi

            clean_url="${url%%#*}"
            echo "Fetching $clean_url"

            curl -L -s --max-time 20 "$clean_url" -o tmp.txt || continue

            if [ -s tmp.txt ]; then
              base64 -d tmp.txt >> build/output.txt 2>/dev/null || cat tmp.txt >> build/output.txt
              echo "" >> build/output.txt
            fi

          done < "$GITHUB_WORKSPACE/NECHAEV_VPN.txt"

          rm -f tmp.txt

      - name: Remove duplicates
        run: |
          sort -u build/output.txt -o build/output.txt

      - name: Check alive proxies
        run: |
          rm -f build/alive.txt
          touch build/alive.txt

          while IFS= read -r proxy; do
            if [ -z "$proxy" ]; then
              continue
            fi

            host=$(echo $proxy | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}')
            port=$(echo $proxy | grep -oE ':[0-9]+' | head -1 | tr -d ':')

            if [ -z "$host" ] || [ -z "$port" ]; then
              continue
            fi

            timeout 3 bash -c "</dev/tcp/$host/$port" 2>/dev/null

            if [ $? -eq 0 ]; then
              echo "$proxy" >> build/alive.txt
              echo "Alive: $host:$port"
            fi

          done < build/output.txt

          mv build/alive.txt build/output.txt

      - name: Encode subscription
        run: |
          base64 build/output.txt > build/subscription.txt

      - name: Commit and push
        run: |
          git config --global user.name "vpn-bot"
          git config --global user.email "vpn-bot@users.noreply.github.com"
          git add build/*
          git commit -m "Update VPN subscription" || echo "No changes"
          git push
